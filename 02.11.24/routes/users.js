import express from 'express';
import User from '../models/User.model.js';

const router = express.Router();

// Similar ao `jokes.js`, com rotas CRUD específicas para `users`

export default router;
